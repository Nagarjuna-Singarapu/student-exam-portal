# Django
DJANGO_SECRET_KEY=change-this-in-production
DJANGO_DEBUG=True

# CORS (comma-separated origins)
CORS_ALLOWED_ORIGINS=http://localhost:5173

# MySQL (local install or Docker—see Section 4)
DB_NAME=examdb
DB_USER=examuser
DB_PASSWORD=exam_pass_123
DB_HOST=127.0.0.1
DB_PORT=3306

# JWT (optional extra tuning later)
ACCESS_TOKEN_LIFETIME_MIN=1440